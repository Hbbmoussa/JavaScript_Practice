//Arrow functions
const myFunc = () => {
  const myVar = "Value";
  return myVar;
};
/**
 *
 * When there is no function body, and only a return value, arrow
 * function syntax allows you to omit the keyword return
 * as well as the brackets surrounding the code.
 */
const valueFunc = () => "value";
